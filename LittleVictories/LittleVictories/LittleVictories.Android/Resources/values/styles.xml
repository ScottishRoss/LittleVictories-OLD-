<?xml version="1.0" encoding="utf-8" ?>
<resources>

  <style name="MainTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
    <item name="windowNoTitle">true</item>
    <item name="android:textAllCaps">false</item> <!-- Enforce buttons to not be full caps -->
    <item name="windowActionBar">false</item> <!--We will be using the toolbar so no need to show ActionBar-->
    <item name="colorPrimary">@color/primary</item><!-- colorPrimary is used for the default action bar background -->
    <item name="android:windowBackground">@color/window_background</item>
    <item name="android:windowTranslucentStatus">true</item>
    <item name="colorAccent">@color/primary</item>
    <item name="android:windowDisablePreview">true</item>
    <item name="android:colorActivatedHighlight">@android:color/transparent</item>>
    <item name="windowActionModeOverlay">true</item>
  </style>

  <style name="SplashTheme" parent ="Theme.AppCompat.Light.NoActionBar">
    <item name="android:windowBackground">@drawable/splash_screen</item>
    <item name="android:windowNoTitle">true</item>
    <item name="android:windowFullscreen">true</item>
    <item name="android:windowAnimationStyle">@null</item>
  </style>

</resources>